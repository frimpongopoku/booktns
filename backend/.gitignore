node_modules/
dist/
*.tsbuildinfo

# Generated Prisma client — regenerated by postinstall and the build.
src/generated/

# Local-disk fallback for verification ID scans when
# CLOUDFLARE_R2_PRIVATE_BUCKET is unset (dev only).
.private-uploads/

.env
.env.*
!.env.example
